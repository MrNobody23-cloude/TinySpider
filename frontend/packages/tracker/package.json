{
    "name": "@insight-os/tracker",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "esbuild src/insight.js --bundle --minify --outfile=dist/insight.min.js && node scripts/check-size.mjs"
    },
    "devDependencies": {
        "esbuild": "^0.25.0"
    }
}